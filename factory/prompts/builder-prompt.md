# Optional provider builder contract
Model output is untrusted design input. It cannot select commands or establish success.
Never include credentials, placeholders, fake integrations, or claims of validation.
The current implementation uses reviewed deterministic blueprints and requires no API key.
A future provider must read credentials from its provider-specific environment variable only.
